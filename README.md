
php artisan db:seed --class=UserSeeder 

touch database/database.sqlite



## create database 
touch database/database.sqlite

## Git Hub Pushing
    0 => git add -u // remove deleted files
    git commit -m "Remove deleted files"
    1 => git add .
    2 => git commit -m 'updates'
    3 => git branch -M app-apis
    4 => git push -u origin app-apis

